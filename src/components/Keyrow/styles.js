import styled from 'styled-components';

import { KeyrowUnstyled } from './Keyrow';

export const Keyrow = styled(KeyrowUnstyled)`
    display: flex;
    align-items: center;
    justify-content: space-around;
`;